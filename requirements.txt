# WeatherStar 4000 Python - Optimized Requirements
# Works on Raspberry Pi 3B+, 4, and Zero 2 W

# Core Requirements (minimal for Pi performance)
pygame==2.5.2       # Display rendering, audio playback
requests==2.31.0    # API calls to weather services

# Optional but recommended
ephem>=4.1.0        # Accurate astronomy calculations (sun/moon data)

# Note for Raspberry Pi users:
# For best performance, install via apt:
# sudo apt-get install python3-pygame python3-requests